PORT=4000
DATABASE_URL=../database/otr.db
FRONTEND_ORIGIN=http://localhost:5173

# Demo-only secret used to sign prototype auth tokens. Replace with a
# real random value if this project is ever deployed anywhere beyond
# a local SIH demo.
JWT_SECRET=otr-india-demo-secret-change-me
JWT_EXPIRES_IN=7d
